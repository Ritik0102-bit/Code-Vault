#include<stdio.h>
#include<conio.h> // this header file is used for these all unformatted specifiers
int main(){
    char ch;
    ch=getchar();
    printf("\n The character displayed is \n");
    putchar(ch);
    // ch=getch();
    // ch=getche();
    // putch(ch);
    // printf("\n Entered charcter is:%c",ch);
    // char name[20];
    // gets(name);
    // puts(name);
}